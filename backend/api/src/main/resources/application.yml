server:
  port: 8080

spring:
  profiles:
    active: local  # 기본은 local 환경
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [traceId=%X{traceId}] [eventId=%X{eventId}] [userId=%X{userId}] [ip=%X{clientIp}] %-5level %logger{36} - %msg%n"

chat:
  redis:
    enabled: true

trip:
  realtime:
    enabled: true

tribe:
  auth:
    enabled: true
  seed:
    enabled: ${TRIBE_SEED_ENABLED:false}
  community:
    image:
      enabled: true
  itinerary:
    place-search:
      enabled: true
  trip:
    review:
      enabled: true
